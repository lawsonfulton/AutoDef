{
  "mesh": "../meshes/veryfinebunny.mesh",

  "training_dataset": "../training_data/very-fine-bunny/training",
  "validation_dataset": "",

  "learning_config": {
    "skip_training": false,
    "skip_jacobian": false,
    "autoencoder_config": {
      "pca_compare_dims": [5, 10, 20],

      "pca_layer_dim": 20,
      "non_pca_layer_sizes": [100],
      "ae_encoded_dim": 4,
      "activation": "elu",

      "batch_size": 500,
      "training_epochs": 600,
      "do_fine_tuning": false,
      "loss_weight": 1.0
    },
    "energy_model_config": {
      "enabled": true,
    }
  }
}