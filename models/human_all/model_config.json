{
  "training_dataset": "../training_data/experimental/human",
  "validation_dataset": "",

  "learning_config": {
    "save_objs": false,
    "skip_training": false,
    "skip_jacobian": false,
    "use_mass_pca": true,
    "autoencoder_config": {
      "pca_compare_dims": [60],

      "pca_layer_dim": 60,
      "non_pca_layer_sizes": [200, 200],
      "ae_encoded_dim": 10,
      "activation": "elu",

      "learning_rate": 0.01,
      "batch_size": 80,
      "training_epochs": 1,
      "do_fine_tuning": false,
      "loss_weight": 1.0
    },
    "energy_model_config": {
      "enabled": false,
      "pca_dim": 40,
      "num_sample_tets": 60,
      "brute_force_iterations": 100,
      "target_anneal_mins": 5
    }
  }
}