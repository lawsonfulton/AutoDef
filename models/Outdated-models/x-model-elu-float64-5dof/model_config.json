{
  "mesh": "../meshes/X.1.mesh",

  "training_dataset": "../training_data/x_interaction/training",
  "validation_dataset": "../training_data/x_interaction/validation",

  "skip_training": false,
  "skip_jacobian": false,
  "autoencoder_config": {
    "pca_compare_dims": [5, 10, 20],

    "pca_layer_dim": 30,
    "non_pca_layer_sizes": [200, 200],
    "ae_encoded_dim": 6,
    "activation": "elu",

    "batch_size": -1,
    "training_epochs": 600,
    "do_fine_tuning": false
  }
}